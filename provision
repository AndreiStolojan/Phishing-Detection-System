#!/usr/bin/env bash
set -Eeuo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "${ROOT}"

log() { printf '\n==> %s\n' "$1"; }
fail() { printf 'Provisioning error: %s\n' "$1" >&2; exit 1; }

DOCKER=(docker)

wait_for_docker() {
  for _ in {1..60}; do
    if docker info >/dev/null 2>&1; then
      return
    fi
    sleep 2
  done
  if pgrep -f com.docker.backend >/dev/null 2>&1; then
    fail "Docker Desktop is still starting or waiting for an administrator prompt. Complete its setup, then run ./provision again."
  fi
  fail "Docker did not become available. Start Docker Desktop and try again."
}

prepare_docker() {
  case "$(uname -s)" in
    Darwin)
      if ! command -v docker >/dev/null 2>&1; then
        fail "Docker Desktop is required: https://docs.docker.com/desktop/setup/install/mac-install/"
      fi
      if ! docker info >/dev/null 2>&1; then
        [[ -d /Applications/Docker.app ]] || fail "Docker Desktop is installed but could not be found in /Applications."
        log "Starting Docker Desktop"
        open -a Docker
        wait_for_docker
      fi
      ;;
    Linux)
      if ! command -v docker >/dev/null 2>&1 || ! docker compose version >/dev/null 2>&1; then
        [[ -r /etc/os-release ]] || fail "Cannot identify this Linux distribution."
        # shellcheck disable=SC1091
        source /etc/os-release
        [[ "${ID:-}" == "ubuntu" ]] || fail "Automatic Docker installation is supported on Ubuntu only."
        command -v sudo >/dev/null 2>&1 || fail "sudo is required to install Docker."

        log "Installing Docker Engine and Docker Compose"
        sudo apt-get update
        sudo apt-get install -y ca-certificates curl openssl
        sudo install -m 0755 -d /etc/apt/keyrings
        sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
        sudo chmod a+r /etc/apt/keyrings/docker.asc
        . /etc/os-release
        printf '%s\n' \
          'Types: deb' \
          'URIs: https://download.docker.com/linux/ubuntu' \
          "Suites: ${UBUNTU_CODENAME:-${VERSION_CODENAME}}" \
          'Components: stable' \
          "Architectures: $(dpkg --print-architecture)" \
          'Signed-By: /etc/apt/keyrings/docker.asc' |
          sudo tee /etc/apt/sources.list.d/docker.sources >/dev/null
        sudo apt-get update
        sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
        sudo systemctl enable --now docker
        sudo usermod -aG docker "${SUDO_USER:-${USER}}"
      fi

      if ! docker info >/dev/null 2>&1; then
        command -v sudo >/dev/null 2>&1 || fail "Current user cannot access Docker."
        sudo docker info >/dev/null 2>&1 || fail "Docker daemon is not available."
        DOCKER=(sudo docker)
      fi
      ;;
    *)
      fail "Supported platforms are macOS and Ubuntu."
      ;;
  esac

  "${DOCKER[@]}" compose version >/dev/null 2>&1 ||
    fail "Docker Compose plugin is required."
}

create_env() {
  command -v openssl >/dev/null 2>&1 || fail "OpenSSL is required to generate local secrets."
  umask 077

  if [[ ! -f .env ]]; then
    log "Generating local configuration"
    cp .env.example .env
  else
    log "Keeping existing .env and filling missing local values"
  fi

  set_env_value() {
    local key="$1" value="$2" current temporary_file
    current="$(sed -nE "s/^${key}=(.*)$/\1/p" .env | tail -n 1)"
    [[ -n "${current}" && "${current}" != replace-with-a-generated-value ]] && return

    temporary_file="$(mktemp "${ROOT}/.env.tmp.XXXXXX")"
    awk -v key="${key}" -v value="${value}" '
      BEGIN { found = 0 }
      index($0, key "=") == 1 {
        if (!found) print key "=" value
        found = 1
        next
      }
      { print }
      END { if (!found) print key "=" value }
    ' .env > "${temporary_file}"
    chmod 0600 "${temporary_file}"
    mv "${temporary_file}" .env
  }

  set_env_value APP_PORT 8080
  set_env_value MONGO_DATABASE secureinbox
  set_env_value MONGO_ROOT_USERNAME secureinbox_root
  set_env_value MONGO_ROOT_PASSWORD "$(openssl rand -hex 32)"
  set_env_value JWT_SECRET "$(openssl rand -hex 32)"
  set_env_value JWT_EXPIRES_IN 8h
  set_env_value MAIL_TOKEN_ENCRYPTION_KEY "$(openssl rand -hex 32)"
  set_env_value DEMO_USER_NAME '"Demo User"'
  set_env_value DEMO_USER_EMAIL demo@secureinbox.test
  set_env_value DEMO_USER_PASSWORD "$(openssl rand -hex 12)"
  set_env_value GRAFANA_ADMIN_USER admin
  set_env_value GRAFANA_ADMIN_PASSWORD "$(openssl rand -hex 16)"
  set_env_value AI_SEMANTIC_ENABLED true
  set_env_value OLLAMA_MODEL qwen2.5:1.5b-instruct-q4_K_M
  set_env_value OLLAMA_TIMEOUT_MS 90000
  set_env_value OLLAMA_PROMPT_VERSION semantic-v3
  set_env_value SCAN_CONCURRENCY 1
  set_env_value SYNC_INTERVAL_MINUTES 15
  set_env_value GOOGLE_REDIRECT_URI http://localhost:8080/api/v1/mail-accounts/google/callback
  set_env_value ARCJET_ENV development
  chmod 0600 .env
}

wait_for_url() {
  local name="$1" url="$2"
  for _ in {1..60}; do
    if curl --fail --silent "${url}" >/dev/null 2>&1; then
      printf '%-12s OK\n' "${name}:"
      return
    fi
    sleep 2
  done
  "${DOCKER[@]}" compose ps
  "${DOCKER[@]}" compose logs --no-color --tail=80
  fail "${name} did not become available at ${url}"
}

wait_for_service_health() {
  local service="$1" container_id status
  for _ in {1..60}; do
    container_id="$("${DOCKER[@]}" compose ps -q "${service}")"
    if [[ -n "${container_id}" ]]; then
      status="$("${DOCKER[@]}" inspect --format '{{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}}' "${container_id}")"
      [[ "${status}" == "healthy" ]] && return
      if [[ "${status}" == "unhealthy" || "${status}" == "exited" || "${status}" == "dead" ]]; then
        "${DOCKER[@]}" compose logs --no-color --tail=80 "${service}"
        fail "${service} entered state: ${status}"
      fi
    fi
    sleep 2
  done
  "${DOCKER[@]}" compose logs --no-color --tail=80 "${service}"
  fail "${service} did not become healthy."
}

main() {
  prepare_docker
  command -v curl >/dev/null 2>&1 || fail "curl is required."
  create_env

  set -a
  # shellcheck disable=SC1091
  source .env
  set +a

  log "Validating and building the local stack"
  "${DOCKER[@]}" compose config --quiet
  "${DOCKER[@]}" compose build --pull
  "${DOCKER[@]}" compose up -d

  log "Waiting for SecureInbox"
  wait_for_url "Application" "http://127.0.0.1:${APP_PORT}/api/v1/ready"

  log "Preparing the local Ollama model"
  if ! "${DOCKER[@]}" compose exec -T ollama ollama list |
    awk 'NR > 1 { print $1 }' |
    grep -Fxq "${OLLAMA_MODEL}"; then
    "${DOCKER[@]}" compose exec -T ollama ollama pull "${OLLAMA_MODEL}"
  fi

  log "Creating the local demo account and inbox"
  "${DOCKER[@]}" compose exec -T backend npm run seed:local

  log "Checking monitoring"
  wait_for_url "Prometheus" "http://127.0.0.1:9090/-/ready"
  wait_for_url "Grafana" "http://127.0.0.1:3000/api/health"
  for service in mongodb ollama backend frontend prometheus grafana; do
    wait_for_service_health "${service}"
  done
  "${DOCKER[@]}" compose ps

  cat <<EOF

SecureInbox is ready.

Application:  http://localhost:${APP_PORT}
Prometheus:   http://localhost:9090
Grafana:      http://localhost:3000

Demo account:
Email:        ${DEMO_USER_EMAIL}
Password:     ${DEMO_USER_PASSWORD}

Grafana:
User:         ${GRAFANA_ADMIN_USER}
Password:     ${GRAFANA_ADMIN_PASSWORD}
EOF
}

main "$@"
