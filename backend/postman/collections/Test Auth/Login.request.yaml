$kind: http-request
name: Login
url: "{{baseUrlLicenta}}/auth/login"
method: POST
body:
  type: json
  content: |-
    {
        "email": "andreistolojan@gmail.com",
        "password": "123123"
    }
scripts:
  - type: afterResponse
    code: |-
      const res = pm.response.json();

      pm.environment.set("userId", res.data.user._id);
    language: text/javascript
order: 4432749504530594
